pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'echo $USER'
                sh 'docker run hello-world'
                sh 'echo triggred'
                sh 'echo test'
                sh 'echo sucess'
                sh 'echo go 454545'
            }
        }
    }
}
